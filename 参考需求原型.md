# 异地同步观影 - MVP设计文档

## 一、核心需求

两人异地，Android端App，同步播放同一视频流。

**核心功能：**
1. 房主创建房间（6位房间码）
2. 加入房间（输入房间码）
3. 房主输入视频URL，双方加载同一视频
4. 播放/暂停/拖动进度 实时同步
5. 手动点击"同步"按钮强制对齐进度
6. 自动检测偏差>1秒时纠正

---

## 二、技术栈

| 端 | 技术 |
|---|---|
| Android | Kotlin + Jetpack Compose + Material 3 |
| 播放器 | ExoPlayer |
| 后端 | Python FastAPI + WebSocket |
| 存储 | Redis |
| 部署 | Docker Compose |

---

## 三、后端API

### HTTP接口

```
POST /api/room/create → { roomId: "ABC123", wsUrl: "ws://..." }
POST /api/room/join  → { videoUrl, currentPos, isPlaying, wsUrl }
GET  /api/room/check/{roomId} → { exists, memberCount }
```

### WebSocket消息

**客户端→服务端：**
```json
// 心跳（每5秒）
{"type":"heartbeat", "payload":{"position":125.5, "isPlaying":true}}

// 播放控制
{"type":"play", "payload":{"position":125.5}}
{"type":"pause", "payload":{"position":125.5}}
{"type":"seek", "payload":{"position":300.0}}

// 切换视频（仅房主）
{"type":"change_video", "payload":{"videoUrl":"..."}}

// 请求强制同步
{"type":"sync_request"}
```

**服务端→客户端：**
```json
// 广播控制指令（同上面play/pause/seek格式）
{"type":"play", "payload":{"position":125.5}}

// 纠正进度（偏差>1秒时下发）
{"type":"correct", "payload":{"targetPosition":125.5}}

// 同步响应
{"type":"sync_response", "payload":{"serverPosition":125.5, "offset":0.2}}

// 视频变更通知
{"type":"video_changed", "payload":{"videoUrl":"..."}}
```

### Redis数据结构

```
room:{roomId} → Hash: {host_id, video_url, current_pos, is_playing, member_count}
room:{roomId}:members → Set: [user_xxx, user_yyy]
```

### 心跳与纠偏逻辑

每5秒收到心跳，比对 `position` 与服务器存储的 `current_pos`。差值>1秒则下发 `correct` 指令。同时用最新上报的position更新服务器基准值（仅当isPlaying且position更大时）。

---

## 四、Android端

### 页面

1. **首页**：创建房间按钮 + 6位输入框 + 加入按钮
2. **播放器页**：视频画面 + 底部控制栏（播放/暂停/进度条/同步按钮）+ 顶部显示房间号和离开按钮

房主额外显示"修改视频链接"入口。

### 播放器封装

```kotlin
class ExoPlayerManager {
    fun init(context)
    fun setVideoUrl(url: String, positionMs: Long = 0)
    fun play()
    fun pause()
    fun seekTo(positionMs: Long)
    fun getCurrentPosition(): Long
    fun isPlaying(): Boolean
    fun addProgressListener(listener: (Long) -> Unit)
    fun release()
}
```

### WebSocket管理

```kotlin
class WebSocketManager {
    fun connect(url: String)
    fun send(type: String, payload: Map<String, Any>)
    fun sendHeartbeat(positionMs: Long, isPlaying: Boolean)  // 每5秒调用
    fun requestSync()  // 手动同步
    fun disconnect()
    // 回调: onPlay, onPause, onSeek, onCorrect, onSyncResponse, onVideoChanged
}
```

### 同步逻辑

1. 收到服务端 `play/pause/seek` → 执行对应操作
2. 收到 `correct` → 强制seek到targetPosition
3. 本地操作（用户点击）→ 先执行本地，再发送指令到服务端
4. 手动同步按钮 → 发送 `sync_request`，收到响应后显示偏差值，若偏差>0.5秒则执行纠正

**注意**：只有房主的 `change_video` 会广播；加入者无法切换视频。

---

## 五、部署

### docker-compose.yml

```yaml
services:
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - REDIS_URL=redis://redis:6379/0
```

### 后端依赖

```
fastapi, uvicorn, redis, websockets
```

---

## 六、异常处理（简版）

| 场景 | 处理 |
|---|---|
| WebSocket断线 | 自动重连（间隔递增，最多10次） |
| 视频加载失败 | Toast提示，允许重新输入链接 |
| 房间不存在/已满 | Toast提示 |
| 房主离开 | 解散房间，成员收到通知后返回首页 |

---

## 七、开发顺序

1. **后端**：FastAPI项目 → Redis操作 → 房间CRUD → WebSocket连接管理 → 心跳/广播/纠偏
2. **Android**：项目初始化 → 首页UI → 播放器页UI → ExoPlayer封装 → WebSocket集成 → 同步逻辑联调
3. **联调测试** → 打包APK

需要注意的是，你从最开始就要考虑到迭代和调试的需求。出于方便调试的考虑，你需要对外提供一个链接托管最新的apk，便于我的手机下载。
这样每次更新之后，我如果有查看效果的需求，在手机上下载apk，就可以联调前后端。